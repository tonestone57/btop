#include <OS.h>
#include <stdio.h>
int main() {
    team_info ti;
    // This is just to see if it compiles and what fields are there
    // (In reality I can't run it but I can try to find docs or assume)
    return 0;
}
